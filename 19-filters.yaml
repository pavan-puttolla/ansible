- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: Linux, Ansible, Devops,python
    tagsmaps:
     name: ansible
     project: roboshop
     component: catalogue
    tagslist: [

           {
            "key": "name",
            "value": "ansible"
        },
        {
            "key": "project",
            "value": "roboshop"
        },
        {
            "key": "component",
            "value": "catalogue"
        }
    ]



  tasks:
    - name: greetings 
      ansible.builtin.debug:
        msg: "Hello PavanKumar {{ WELCOME | default('how you doing') }}"
    
    - name: convert topics to list
      ansible.builtin.debug:
        msg: "Topics are {{ topics  | split(',') }}"

    - name: convert maps to list
      ansible.builtin.debug:
        msg: "{{  tagsmaps  | dict2items }}"

    - name: convert list to map
      ansible.builtin.debug:
        msg: "{{  tagslist  | items2dict }}"
    
    - name: read student yaml file
      ansible.builtin.set_fact:    # set_fact is used to create variable in ansible
        student_yaml: "{{ lookup ('file', 'students.yaml') | from_yaml }}"
    
    - name: convert studentfile from  yaml to json
      ansible.builtin.debug:
        msg: "{{ student_yaml | to_nice_json }}"

      