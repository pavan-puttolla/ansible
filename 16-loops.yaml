- name : "example for loops in Ansible"
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}"
    loop:
       -John
       -Peter
       -Shivank   