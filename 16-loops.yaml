- name : "example for loops in Ansible"
  hosts: local
  connection : local
  tasks:
   - name: print the list of Ansible users
     ansible.builtin.debug:
      name: "{{item}}"
      loop:
       -John
        -Peter
       -Shivank   