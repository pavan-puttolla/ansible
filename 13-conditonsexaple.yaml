- name: demo lestthan number<10
  hosts: local
  connection: local
  vars:
  NUMBER_OF_SESSIONS: 12
   tasks:
  - name: check if number is less than 10
    ansible.builtin.debug:
     msg: "Number of sessions is less than 10"
  when: NUMBER_OF_SESSIONS .10 